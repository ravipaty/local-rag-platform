# PLAINTEXT PLACEHOLDER — do not commit as-is.
# Seal this with kubeseal before pushing to git:
#   kubeseal --format yaml < secret.yaml > sealed-secret.yaml
# Then commit only sealed-secret.yaml and delete/gitignore this file.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: rag-platform
type: Opaque
stringData:
  POSTGRES_DB: ragdb
  POSTGRES_USER: raguser
  POSTGRES_PASSWORD: changeme
